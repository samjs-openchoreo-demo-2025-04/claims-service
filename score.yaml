apiVersion: score.dev/v1b1
metadata:
  name: claims-service
  project: internal-apps
  organization: claims-service
service:
  ports:
    default:
      port: 8080
      protocol: TCP
containers:
  container-one1:
    image: .